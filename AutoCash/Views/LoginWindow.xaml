<Page x:Class="AutoCash.Views.LoginWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AutoCash.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginWindow">

    <Grid>
        <Border Background="White" CornerRadius="8" Margin="25" Padding="20"
                BorderBrush="#DFE4EC" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="15" Opacity="0.1" ShadowDepth="3"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="ОКНО ВХОДА" FontSize="18" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,15" Foreground="#2C3E50"/>

                <TextBlock Text="Логин сотрудника:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLogin" Height="30" Padding="4" Margin="0,0,0,12" FontSize="14"/>

                <TextBlock Text="Пароль:" FontSize="12" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="30" Padding="4" Margin="0,0,0,20" FontSize="14"/>

                <Button x:Name="btnLogin" Content="ВОЙТИ В СИСТЕМУ" Height="38" FontSize="13" FontWeight="Bold"
                        Background="#2ECC71" Foreground="White" BorderThickness="0"
                        Click="btnLogin_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Page>
