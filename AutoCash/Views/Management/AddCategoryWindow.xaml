<Window x:Class="AutoCash.Views.Management.AddCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новая категория" Height="200" Width="360"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" 
        Background="#F8FAFC" KeyDown="Window_KeyDown">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="НАЗВАНИЕ НОВОЙ КАТЕГОРИИ" FontSize="11" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,8"/>

        <TextBox x:Name="txtCategoryName" Grid.Row="1" Height="38" Padding="8,6" FontSize="14" Foreground="#1E293B">
            <TextBox.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </TextBox.Resources>
        </TextBox>

        <Grid Grid.Row="2" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnCancel" Grid.Column="0" Content="Отмена (Esc)" Height="35" Margin="0,0,5,0" 
                    Background="#64748B" Click="btnCancel_Click"/>

            <Button x:Name="btnSave" Grid.Column="1" Content="Сохранить (Enter)" Height="35" Margin="5,0,0,0" 
                    Background="#10B981" FontWeight="Bold" Click="btnSave_Click"/>
        </Grid>
    </Grid>
</Window>