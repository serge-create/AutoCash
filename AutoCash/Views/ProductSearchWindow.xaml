<Window x:Class="AutoCash.Views.ProductSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Визуальный поиск товара" Height="500" Width="700"
        WindowStartupLocation="CenterOwner" Background="#F8FAFC"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="НАИМЕНОВАНИЕ ИЛИ ШТРИХКОД (F3 для фокуса)" FontSize="12" FontWeight="SemiBold" Foreground="#64748B" Margin="0,0,0,5"/>
            <TextBox x:Name="txtSearch" Height="40" Padding="10,8" FontSize="16" TextChanged="txtSearch_TextChanged">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="5">
            <DataGrid x:Name="dgProducts" AutoGenerateColumns="False" IsReadOnly="True" 
                      HeadersVisibility="Column" FontSize="14" RowHeight="35"
                      Background="White" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0"
                      MouseDoubleClick="dgProducts_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Штрихкод" Binding="{Binding Barcode}" Width="120"/>
                    <DataGridTextColumn Header="Наименование товара" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Остаток" Binding="{Binding StockQuantity}" Width="80"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat={}{0:F2} ₽}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="*Дважды кликните по строке или нажмите Enter для выбора" 
                       VerticalAlignment="Center" Foreground="#94A3B8" FontSize="11" FontStyle="Italic"/>

            <Button x:Name="btnCancel" Grid.Column="1" Content="ОТМЕНА (Esc)" Width="120" Height="40" Margin="0,0,10,0"
                    Background="#EF4444" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="btnCancel_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="btnSelect" Grid.Column="2" Content="ВЫБРАТЬ (Enter)" Width="150" Height="40"
                    Background="#22C55E" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="btnSelect_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>